services:
  server:
    container_name: miinodo
    build: .
    tty: true
    stdin_open: true
    volumes:
      - ./work:/penguinpanthers:Z
    command: ["bash", "--rcfile", "/penguinpanthers/.bashrc", "-i"]
